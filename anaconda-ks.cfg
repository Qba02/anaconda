# Generated by Anaconda 38.23.4
# Generated by pykickstart v3.47
#version=F38
# Use graphical install
graphical

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8

# Repo
url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-38&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f38&arch=x86_64

%packages
@^server-product-environment
docker
%end

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.7.1
ignoredisk --only-use=sda
autopart
# Partition clearing information
clearpart --all

# System timezone
timezone Europe/Warsaw --utc

# Root password
rootpw --iscrypted $y$j9T$Nu2xNiNi8jRy9SLdobaWAWMM$6.zgeKW27rNHJs0xI0RLmp0MVRVT336hrn2R3JCWhtD

%post --erroronfail

# Dodanie użytkownika "root" do grupy "docker"
usermod -aG docker root

# Włączenie i uruchomienie usługi Docker
systemctl enable docker
systemctl start docker

# Skrypt do uruchomienia kontenera Docker
cat <<'EOS' > /root/run_docker.sh
#!/bin/bash
docker pull qba002/jestapp
docker run -d -p 41247:3000 --name deploy qba002/jestapp:latest
EOS

# Nadanie uprawnien
chmod +x /root/run_docker.sh

# Ustawienie skrypt w cronie, aby uruchomil sie po pierwszym uruchomieniu systemu
echo "@reboot /root/run_docker.sh" >> /etc/crontab

%end

